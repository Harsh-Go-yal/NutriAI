# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9        # meal-photo uploads (multipart form parsing)

# Config / validation
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# LLM
openai>=1.50.0                 # the only model provider; Groq/Ollama removed

# Database
sqlalchemy>=2.0.30
alembic>=1.13.0
psycopg2-binary>=2.9.9         # only needed if you use Postgres, not SQLite

# Optimisation
scipy>=1.14.0                  # linprog/HiGHS for the nutrition-per-rupee solver

# HTTP (USDA lookups, Sarvam calls, health checks)
httpx>=0.27.0

# Optional — not imported by the app today, kept for the Docker compose stack
redis>=5.0.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.6.0
black>=24.0.0
